pre-commit:
  commands:
    demo:
      run: 'echo "これからコミットを行います!"'

commit-msg:
  commands:
    demo:
      run: 'echo "コミットメッセージ!"'
    # check-message:
    #   run: bash -c 'echo "メッセージNG"; exit 1'

post-commit:
  commands:
    demo:
      run: 'echo "コミットが完了しました!"'

pre-push:
  commands:
    demo:
      run: 'echo "これからpushします!"'